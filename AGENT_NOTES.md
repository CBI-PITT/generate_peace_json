# Agent Notes

- 2026-06-12: Updated Flask `AGENTS.md` to require reading root and Flask notes before changes and appending Flask notes after subproject file changes.
- 2026-06-15: Updated `form.html`, `form_image_calculator.html`, and `create_workflow.html` to remember the last selected browser folder within the current form page and reopen subsequent pickers there.
- 2026-06-15: Added Flask reader plugin `ome_zarr_reader_plugin.py` so `.ome.zarr` readers can be submitted with `channel` and `resolution_level` like `omehans_reader`.
- 2026-08-27: Updated workflow-template rendering so numeric Flask form fields stay editable after template save/load, and float values like stretch-contrast percentiles keep their decimal values.
- 2026-08-27: Fixed workflow JSON generation so non-reader steps backfill missing `extras.output` from the input folder's `.dataset_info.json` `base_output_dir` when available.
- 2026-08-27: Added optional Flask job/workflow history support behind `PEACE_ENABLE_JOB_HISTORY`, including submission IDs, history JSON records, Home-page `My jobs` / `My workflows` sections, SLURM-backed status display, and cancel/hold/release/rerun endpoints with workflow rerun-from-step fork support.
- 2026-08-27: Fixed Flask history ownership so `submitted_by` stays the logged-in account, while `extras.user` still uses the path-derived execution user for `CBI_Admin` submissions and preserves existing SLURM job-name prefixes.
- 2026-09-02: Added preprocessing-only `z_start` and `z_end` integer fields with `0`/`-1` defaults, direct-form and workflow validation, image-calculator rendering, and legacy saved-workflow compatibility for all nine preprocessing operations.
- 2026-09-08: Restored standalone operation output-folder autofill after browser input selection, with stale-request and manual-output safeguards; workflow fragments, calculator handlers, and server-side workflow output derivation remain unchanged.
- 2026-09-10: Added `MeanIntensityForm` in `flask_app/forms.py` (hidden input/output; bindable browse fields `cells_path` and `image_path`; workflow-editable `radius` FloatField in um) and the `MeanIntensity` plugin in `flask_app/operations/mean_intensity.py` (`category = "post_processing"`, `get_username` from `cells_path`, default `form.html`). No workflow-code changes needed; the operation appears automatically in the post_processing group.
- 2026-09-15: Made the `mean_intensity` form provenance-only by removing required `image_path`; submissions now include just cells path, radius, and standard priority/user fields. Updated the operation description to explain automatic raw-source resolution.
- 2026-09-16: Redesigned `create_workflow.html` workflow composition: the operation selector moved into the Workflow Steps card below the header, next to a `Step N:` pending label, with the operation form auto-loading on selection change (Load Settings button removed; clearing the selection empties the form). `renderWorkflowSteps()` is now the single pending-row/submit-state sync point (label advance, selector reset, form-container clear, controls hide, output-name reset, submit disabled with zero steps), and the add-step reset lines were folded into it. Each committed step gains a remove (X) button that splices the step, renumbers via re-render, and strips dangling input bindings to its output unless another remaining step produces the same output name. Save/load template handlers, browse buttons/iframes, editable and mutable field rendering, and the `workflow` JSON shape are unchanged.
- 2026-09-16: Added the same-form `nearest_neighbor` operation: `NearestNeighborForm` in `forms.py` (hidden `input`/`output`/`operation`, `cells_path` workflow path field, mirroring `TransformPointsForm`) and `NearestNeighbor` plugin in `operations/nearest_neighbor.py` (category `post_processing`, auto-discovered, default `form.html` template, username derived from `cells_path`). Standalone-only like other post-processing ops; JSON contract untouched.
- 2026-09-21: Added the same-form `remove_background` operation: parameter-free `RemoveBackgroundForm(PreProcessingForm)` in `forms.py` (hidden `operation` default `remove_background`; inherits `input`/`output`/`priority`/`z_start`/`z_end`, mirroring `RembgForm`) and the `RemoveBackground` plugin in `operations/remove_background.py` (`category = "pre_processing"`, `form_autofill_output.html`, auto-discovered). Extras emitted: `priority`, `z_start`, `z_end` (+ `user`), matching the new backend `operations/remove_background/main.py` kwargs. Verified with the `peace-flask` env: class import, form defaults (`priority='2'`, `z_start=0`, `z_end=-1`), template, and app-style `load_plugins('operations')` discovery (appears in the pre_processing category and workflow builder automatically).
- 2026-09-22: Replaced the inline file-browser iframes with a modal picker. New templates/browser_modal.html (#fileBrowserModal, one chrome-less iframe) and static/js/browser_picker.js: delegated .browse-btn handler opens the modal at /browser/dir_embed/ and sets the iframe data-field-id; its message handler fills hidden inputs and dispatches input (keeps output autofill), updates #selected-name labels, syncs workflow.steps extras for data-step-index fields, closes the modal, and remembers nextBrowserPath. form.html, form_image_calculator.html (3 copies deduplicated), macros/form_fragment_macro.html, and create_workflow.html now render only Browse buttons; duplicated toggle/message JS removed (create_workflow setupBrowseButtons/updateBrowserLocation/message handler replaced by the shared JS). form_autofill_output.html calls super() in its scripts block so the modal + picker load for the ~17 operation forms extending it. JSON contract untouched; verified all form pages, workflow fragments, and the unchanged full-page browser via test-client renders.
